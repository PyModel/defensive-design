# Reviewed test baseline. No dependency is required to read/use the text skill.
# Full resolved HTTPX graph from CI plus the safe-YAML package validator dependency.
# CI records Python and package versions; pins are not an integrity/provenance proof.
anyio==4.15.0
certifi==2026.7.22
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
PyYAML==6.0.3
typing_extensions==4.16.0
